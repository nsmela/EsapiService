<Window x:Class="TestWpf.Ui.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TestWpf.Ui"
        Title="Async ESAPI Test" Height="450" Width="800"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Async ESAPI Proxy Test" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>

        <Button Grid.Row="1" Command="{Binding LoadPatientCommand}" IsEnabled="{Binding IsNotBusy}"
                Content="Load Patient (Hardcoded ID)" HorizontalAlignment="Left" Padding="10,5"/>

        <Grid Grid.Row="2" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Column 1: Plans -->
            <StackPanel Grid.Column="0" Margin="5">
                <TextBlock Text="Plans" FontWeight="Bold"/>
                <ListBox ItemsSource="{Binding Plans}"
                         SelectedItem="{Binding SelectedPlan}"
                         DisplayMemberPath="Id"
                         Height="300"
                         IsEnabled="{Binding IsNotBusy}"/>
            </StackPanel>

            <!-- Column 2: Structures -->
            <StackPanel Grid.Column="1" Margin="5">
                <TextBlock Text="Structures (Non-Empty)" FontWeight="Bold"/>
                <ListBox ItemsSource="{Binding Structures}"
                         SelectedItem="{Binding SelectedStructure}"
                         DisplayMemberPath="Id"
                         Height="300"
                         IsEnabled="{Binding IsNotBusy}"/>
            </StackPanel>

            <!-- Column 3: Details -->
            <StackPanel Grid.Column="2" Margin="5">
                <TextBlock Text="Details" FontWeight="Bold"/>
                <ListBox ItemsSource="{Binding StructureDetails}"
                         Height="300"
                         IsEnabled="{Binding IsNotBusy}"/>
            </StackPanel>

        </Grid>

        <!-- Busy Indicator -->
        <Border Grid.Row="0" Grid.RowSpan="3"
                Background="#88000000"
                Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}">
            <TextBlock Text="Loading..." FontSize="24" FontWeight="Bold"
                       Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Border>

    </Grid>
</Window>
